{
  "name": "codex-ledger",
  "version": "0.2.0",
  "private": true,
  "description": "Audit-oriented SaaS accounting platform with ASC 606, receivables, SaaS metrics, and AI-assisted journal drafting",
  "type": "module",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "dev": "node --env-file-if-exists=.env --watch server.js",
    "test": "node --test",
    "lint": "eslint . && prettier --check .",
    "format": "prettier --write .",
    "migrate": "node scripts/migrate.js",
    "migrate:down": "node scripts/migrate.js --down",
    "migrate:rehearse": "node scripts/migration-rehearsal.js",
    "backup": "node scripts/backup.js",
    "restore": "node scripts/restore.js",
    "verify-integrity": "node scripts/verify-integrity.js",
    "build": "vite build",
    "test:load": "node scripts/load-benchmark.js",
    "integrations:sync": "node --env-file-if-exists=.env scripts/sync-integration.js",
    "accounting-validation": "node scripts/accounting-validation.js",
    "ops:preflight": "node scripts/ops-preflight.js",
    "privacy": "node scripts/privacy.js"
  },
  "engines": {
    "node": "22.23.2"
  },
  "packageManager": "npm@11.6.2",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.9.0",
    "fast-check": "^4.9.0",
    "globals": "^17.11.0",
    "prettier": "^3.9.6",
    "vite": "^8.2.2"
  },
  "dependencies": {
    "@sentry/node": "^10.70.0",
    "argon2": "^0.45.1",
    "pdfkit": "^0.19.1",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "zod": "^4.4.3"
  }
}
